DATABASE_URL=postgres://job_radar:job_radar@localhost:5433/job_radar?sslmode=disable
REDIS_ADDR=localhost:6379
REDIS_DB=0
API_PORT=8080
REMOTIVE_MAX_REQUESTS_PER_DAY=4
FRONTEND_ORIGIN=http://localhost:5173
# Dev-only placeholder. Generate a real secret for any non-local deployment,
# e.g. `openssl rand -base64 32`.
JWT_SECRET=dev-only-insecure-secret-change-me
